export function initSmoothScroll() {
  document.documentElement.style.scrollBehavior = 'smooth';

  // Next step: replace this with Lenis if you want smoother, cinematic easing.
  // Next step: add fixed-header offset handling for hash navigation.
}
